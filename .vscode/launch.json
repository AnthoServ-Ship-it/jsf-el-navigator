{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Ejecutar JSF EL Navigator",
            "type": "extensionHost",
            "request": "launch",
            "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
            "outFiles": ["${workspaceFolder}/dist/**/*.js"],
            "preLaunchTask": "Compilar extensión"
        }
    ]
}
